FROM node:18-alpine

# Create app directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install --production

# Copy application code
COPY . .

# App runs on port 3000 (change ONLY if your app uses different)
EXPOSE 3000

# Start the app
CMD ["node", "index.js"]
